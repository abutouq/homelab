apiVersion: v1
kind: Service
metadata:
  name: {{ include "kids-app.fullname" . }}
  labels:
    {{- include "kids-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "kids-app.selectorLabels" . | nindent 4 }}
