apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "kids-app.fullname" . }}-test-connection"
  labels:
    {{- include "kids-app.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "kids-app.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
