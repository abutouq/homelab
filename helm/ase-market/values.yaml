# Default values for ase-market.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

# This will set the replicaset count more information can be found here: https://kubernetes.io/docs/concepts/workloads/controllers/replicaset/
replicaCount: 1

# This sets the container image more information can be found here: https://kubernetes.io/docs/concepts/containers/images/
image:
  repository: aabutouq/ase-stock-api
  # This sets the pull policy for images.
  pullPolicy: IfNotPresent
  # Overrides the image tag whose default is the chart appVersion.
  tag: "v1.0.13"

# This is for the secrets for pulling an image from a private repository more information can be found here: https://kubernetes.io/docs/tasks/configure-pod-container/pull-image-private-registry/
imagePullSecrets: [
  name: aabutouq-registry-secret
]
# This is to override the chart name.
nameOverride: ""
fullnameOverride: ""

# This section builds out the service account more information can be found here: https://kubernetes.io/docs/concepts/security/service-accounts/
serviceAccount:
  # Specifies whether a service account should be created.
  create: true
  # Automatically mount a ServiceAccount's API credentials?
  automount: true
  # Annotations to add to the service account.
  annotations: {}
  # The name of the service account to use.
  # If not set and create is true, a name is generated using the fullname template.
  name: ""

# This is for setting Kubernetes Annotations to a Pod.
# For more information checkout: https://kubernetes.io/docs/concepts/overview/working-with-objects/annotations/
podAnnotations: {}
# This is for setting Kubernetes Labels to a Pod.
# For more information checkout: https://kubernetes.io/docs/concepts/overview/working-with-objects/labels/
podLabels: {}

podSecurityContext: {}
  # fsGroup: 2000

securityContext: {}
  # capabilities:
  #   drop:
  #   - ALL
  # readOnlyRootFilesystem: true
  # runAsNonRoot: true
  # runAsUser: 1000

# This is for setting up a service more information can be found here: https://kubernetes.io/docs/concepts/services-networking/service/
service:
  # This sets the service type more information can be found here: https://kubernetes.io/docs/concepts/services-networking/service/#publishing-services-service-types
  type: LoadBalancer
  # This sets the ports more information can be found here: https://kubernetes.io/docs/concepts/services-networking/service/#field-spec-ports
  port: 80

# This block is for setting up the ingress for more information can be found here: https://kubernetes.io/docs/concepts/services-networking/ingress/
ingress:
  enabled: true
  className: "nginx"
  annotations: {
    kubernetes.io/ingress.class: nginx,
    nginx.ingress.kubernetes.io/ssl-redirect: "true",
    cert-manager.io/cluster-issuer: letsencrypt-prod
  }
  hosts:
    - host: ase-market.homebytes.space
      paths:
        - path: /
          pathType: Prefix
  tls:
    - secretName: stocks-tls-secret
      hosts:
        - ase-market.homebytes.space

# -- Expose the service via gateway-api HTTPRoute
# Requires Gateway API resources and suitable controller installed within the cluster
# (see: https://gateway-api.sigs.k8s.io/guides/)
httpRoute:
  # HTTPRoute enabled.
  enabled: false
  # HTTPRoute annotations.
  annotations: {}
  # Which Gateways this Route is attached to.
  parentRefs:
  - name: gateway
    sectionName: http
    # namespace: default
  # Hostnames matching HTTP header.
  hostnames:
  - chart-example.local
  # List of rules and filters applied.
  rules:
  - matches:
    - path:
        type: PathPrefix
        value: /headers
  #   filters:
  #   - type: RequestHeaderModifier
  #     requestHeaderModifier:
  #       set:
  #       - name: My-Overwrite-Header
  #         value: this-is-the-only-value
  #       remove:
  #       - User-Agent
  # - matches:
  #   - path:
  #       type: PathPrefix
  #       value: /echo
  #     headers:
  #     - name: version
  #       value: v2

resources:
  limits:
    cpu: 100m
    memory: 128Mi
  requests:
    cpu: 100m
    memory: 128Mi

# This is to setup the liveness and readiness probes more information can be found here: https://kubernetes.io/docs/tasks/configure-pod-container/configure-liveness-readiness-startup-probes/
livenessProbe:
  httpGet:
    path: /
    port: http
readinessProbe:
  httpGet:
    path: /
    port: http

# This section is for setting up autoscaling more information can be found here: https://kubernetes.io/docs/concepts/workloads/autoscaling/
autoscaling:
  enabled: true
  minReplicas: 1
  maxReplicas: 5
  targetCPUUtilizationPercentage: 50
  # targetMemoryUtilizationPercentage: 80

# Additional volumes on the output Deployment definition.
volumes: []
  # - name: foo
  #   secret:
  #     secretName: mysecret
  #     optional: false

# Additional volumeMounts on the output Deployment definition.
volumeMounts: []
  # - name: foo
  #   mountPath: "/etc/foo"
  #   readOnly: true

nodeSelector: {
  node.kubernetes.io/environment: "production"
}

tolerations: []

affinity: {}

ase:
  # Credentials live in a pre-existing Secret named "<fullname>-ase-credentials"
  # (keys: username, password). When secretsSync.enabled is false this Secret
  # must be created out-of-band; when true, the CronJob below keeps it in sync.
  enabled: true

secretsSync:
  # Keeps the "<fullname>-ase-credentials" Secret in sync with an entry in a
  # LocalStack-backed Secrets Manager (see ../../external-services/README.md).
  # Disabled by default: LocalStack isn't deployed yet, and pods only read
  # this Secret's values at container start, so a rotated value still needs
  # a pod restart to take effect (no Reloader-style live-refresh here).
  enabled: false
  image:
    repository: aabutouq/secrets-sync
    tag: v1.0.0
  schedule: "*/15 * * * *"
  localstackEndpoint: "http://secrets-manager.homebytes.space:4566"
  secretId: "ase-market/ase-credentials"
  # targetSecretName defaults to "<fullname>-ase-credentials" when empty
  targetSecretName: ""

registrySecretSync:
  # Keeps the "aabutouq-registry-secret" Secret (referenced by
  # imagePullSecrets above) in sync with a .dockerconfigjson blob stored in
  # LocalStack Secrets Manager (see ../registry-sync/).
  enabled: true
  image:
    repository: aabutouq/registry-secret-sync
    tag: v1.0.0
  schedule: "*/15 * * * *"
  localstackEndpoint: "http://secrets-manager.homebytes.space:4566"
  secretId: "ase-market/docker-registry"
  targetSecretName: "aabutouq-registry-secret"

postgresql:
  enabled: true
  database: stocks
  username: stocks
  password: stocks
  storage: 5Gi
  storageClass: ""
  resources:
    requests:
      cpu: 100m
      memory: 256Mi
    limits:
      cpu: 500m
      memory: 512Mi
