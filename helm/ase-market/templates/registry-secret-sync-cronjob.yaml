{{- if .Values.registrySecretSync.enabled }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "ase-market.fullname" . }}-registry-secret-sync
  labels:
    {{- include "ase-market.labels" . | nindent 4 }}
---
apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: {{ include "ase-market.fullname" . }}-registry-secret-sync
  labels:
    {{- include "ase-market.labels" . | nindent 4 }}
rules:
  - apiGroups: [""]
    resources: ["secrets"]
    verbs: ["get", "create", "update", "patch"]
---
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: {{ include "ase-market.fullname" . }}-registry-secret-sync
  labels:
    {{- include "ase-market.labels" . | nindent 4 }}
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: Role
  name: {{ include "ase-market.fullname" . }}-registry-secret-sync
subjects:
  - kind: ServiceAccount
    name: {{ include "ase-market.fullname" . }}-registry-secret-sync
    namespace: {{ .Release.Namespace }}
---
apiVersion: batch/v1
kind: CronJob
metadata:
  name: {{ include "ase-market.fullname" . }}-registry-secret-sync
  labels:
    {{- include "ase-market.labels" . | nindent 4 }}
    app.kubernetes.io/component: registry-secret-sync
spec:
  schedule: {{ .Values.registrySecretSync.schedule | quote }}
  concurrencyPolicy: Forbid
  successfulJobsHistoryLimit: 3
  failedJobsHistoryLimit: 3
  jobTemplate:
    spec:
      ttlSecondsAfterFinished: 3600
      backoffLimit: 2
      template:
        metadata:
          labels:
            {{- include "ase-market.selectorLabels" . | nindent 12 }}
            app.kubernetes.io/component: registry-secret-sync
        spec:
          restartPolicy: OnFailure
          serviceAccountName: {{ include "ase-market.fullname" . }}-registry-secret-sync
          containers:
            - name: registry-secret-sync
              image: "{{ .Values.registrySecretSync.image.repository }}:{{ .Values.registrySecretSync.image.tag }}"
              imagePullPolicy: IfNotPresent
              env:
                - name: AWS_ACCESS_KEY_ID
                  value: "test"
                - name: AWS_SECRET_ACCESS_KEY
                  value: "test"
                - name: LOCALSTACK_ENDPOINT
                  value: {{ .Values.registrySecretSync.localstackEndpoint | quote }}
                - name: SECRET_ID
                  value: {{ .Values.registrySecretSync.secretId | quote }}
                - name: TARGET_SECRET_NAME
                  value: {{ .Values.registrySecretSync.targetSecretName | quote }}
                - name: TARGET_NAMESPACE
                  value: {{ .Release.Namespace | quote }}
              resources:
                requests:
                  cpu: 25m
                  memory: 32Mi
                limits:
                  cpu: 100m
                  memory: 64Mi
{{- end }}
